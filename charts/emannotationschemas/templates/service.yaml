apiVersion: v1
kind: Service
metadata:
  name: emannotationschemas-service
  labels:
    tier: dynamicmicroservice
spec:
  type: ClusterIP
  selector:
    app: emannotationschemas-app
  ports:
    - port: 80
      targetPort: 80
