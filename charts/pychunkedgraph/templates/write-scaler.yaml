{{- if and .Values.keda.enabled .Values.gmp.enabled }}
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: pychunkedgraph-scaler
  annotations:
    validations.keda.sh/hpa-ownership: "true"
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pychunkedgraph
  minReplicaCount: {{ .Values.pychunkedgraph.writeMinReplicas | default 1 }}
  maxReplicaCount: {{ .Values.pychunkedgraph.writeMaxReplicas | default 10 }}
  triggers:
    - type: prometheus
      authenticationRef:
        name: pcg-keda-trigger-auth-gcp-credentials
      metadata:
        serverAddress: https://monitoring.googleapis.com/v1/projects/{{ .Values.cluster.googleProject }}/location/global/prometheus
        # GMP-mapped Prometheus metric; scope to cluster/namespace and controller
        query: >-
          avg(avg_over_time(uwsgi_perc_busy_workers{cluster="{{ .Values.cluster.cluster }}",namespace="{{ .Release.Namespace }}",top_level_controller_name="pychunkedgraph"}[1m])) or vector(0)
        # Target AverageValue in percent
        threshold: "{{ .Values.pychunkedgraph.apiBusyWorkersTarget | default 75 }}"
    - type: cpu
      metricType: Utilization
      metadata:
        value: "{{ .Values.pychunkedgraph.apiCpuTarget | default 110 }}"
{{- else }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pychunkedgraph-scaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pychunkedgraph
  minReplicas: {{ .Values.pychunkedgraph.writeMinReplicas | default 1}}
  maxReplicas: {{ .Values.pychunkedgraph.writeMaxReplicas | default 10}}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 110
{{- end }}
